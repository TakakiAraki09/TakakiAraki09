# Design System Skill

Panda CSSベースのデザインシステムを管理するClaude Code skillなのだ！

## 概要

このskillは、TakakiAraki09プロジェクトのデザインシステム（`packages/panda-config/`）を効率的に管理するためのツールなのだ。

**主な機能**：
- 🎨 OKLCHカラーの追加・管理
- 🎯 セマンティックトークンの管理（ライト/ダークモード対応）
- 📦 コンポーネントレシピの作成・更新
- 🔧 デザイントークンの拡張
- ✅ デザインシステムの検証・チェック
- 🚀 コンポーネントスタイルの自動生成

## クイックスタート

### 1. 新しい色を追加

```
/design-system add color purple
```

### 2. コンポーネントレシピを作成

```
/design-system add recipe input size,variant
```

### 3. コンポーネントスタイルを生成

```
/design-system create component-style Modal
```

### 4. デザインシステムをチェック

```
/design-system check all
```

## デザインシステムの構造

```
packages/panda-config/
├── config/
│   ├── baseColors.ts          # OKLCHベースカラー（gray, blue, green, red, yellow）
│   ├── colors.ts              # セマンティックトークン（bg, contents, accent）
│   ├── spacing.ts             # 0-49（4px刻み）
│   ├── fontSizes.ts           # body系、heading系、display系
│   ├── radii.ts               # inner, outer, full
│   └── recipes/               # コンポーネントレシピ
│       ├── button.ts
│       ├── card.ts
│       └── header.ts
└── panda.preset.ts
```

## よく使うコマンド

### カラー管理

| コマンド | 説明 |
|---------|------|
| `/design-system add color <name> [hue]` | 新しいベースカラーを追加 |
| `/design-system add semantic-color <category> <name>` | セマンティックカラーを追加 |
| `/design-system check colors` | カラー定義をチェック |

**例**：
```
/design-system add color teal 180
/design-system add semantic-color bg tertiary
```

### レシピ管理

| コマンド | 説明 |
|---------|------|
| `/design-system add recipe <name> [variants]` | 新しいレシピを作成 |
| `/design-system update recipe <name> <action>` | レシピを更新 |
| `/design-system validate recipe <name>` | レシピを検証 |
| `/design-system check recipes` | すべてのレシピをチェック |

**例**：
```
/design-system add recipe input size,variant,state
/design-system validate recipe button
```

### トークン管理

| コマンド | 説明 |
|---------|------|
| `/design-system extend spacing <max>` | スペーシングを拡張 |
| `/design-system add font-size <name> <value>` | フォントサイズを追加 |
| `/design-system add radii <name> <value>` | 角丸を追加 |

**例**：
```
/design-system extend spacing 60
/design-system add font-size display-xl 3rem
/design-system add radii sm 4px
```

### コンポーネント生成

| コマンド | 説明 |
|---------|------|
| `/design-system create component-style <Name>` | コンポーネントスタイルを生成 |
| `/design-system create recipe-component <Name>` | レシピベースのコンポーネントを生成 |

**例**：
```
/design-system create component-style Modal
/design-system create recipe-component Button
```

### 検証・チェック

| コマンド | 説明 |
|---------|------|
| `/design-system check all` | デザインシステム全体をチェック |
| `/design-system check colors` | カラー定義をチェック |
| `/design-system check recipes` | レシピをチェック |
| `/design-system validate component <Name>` | コンポーネントを検証 |

**例**：
```
/design-system check all
/design-system validate component Button
```

## デザイン原則

### 1. セマンティックトークンを使う

❌ **悪い例**：
```typescript
bg: "blue.500"
color: "gray.800"
```

✅ **良い例**：
```typescript
bg: "bg.info"
color: "contents.primary"
```

### 2. レシピで再利用性を高める

同じスタイルパターンが複数箇所で使われる場合、レシピを作るのだ。

### 3. OKLCHカラーを使う

新しい色を追加する場合、OKLCHカラースペースで定義するのだ：
- 知覚的に均等
- 明度のコントロールが直感的
- アクセシビリティに優れている

### 4. 型安全性を保つ

- `as`、`!`、`any`を使わない
- strictTokens、strictPropertyValuesに従う
- MCPツールを活用

## ファイル構成

```
.claude/skills/design-system/
├── SKILL.md       # Skillの実装ガイド（メイン）
├── FORMS.md       # 詳細なAPIリファレンスと実例
└── README.md      # このファイル（概要と使い方）
```

## トラブルシューティング

### レシピが反映されない

```
/design-system check recipes
```
→ `recipes/index.ts`でエクスポートされているか確認
→ `pnpm build`で再ビルド

### トークンが使えない

```
/design-system check tokens
```
→ strictTokens: trueなので未定義のトークンは使えない
→ トークン定義を確認

### 型エラーが出る

MCPの診断ツールで確認：
```typescript
mcp__typescript__get_diagnostics
```

### ライト/ダークモードで表示がおかしい

```
/design-system check colors
```
→ セマンティックトークンに`_light`と`_dark`の両方が定義されているか確認

## 制約事項

- **strictTokens: true**: 未定義のトークンは使えない
- **strictPropertyValues: true**: プロパティ値の型チェックが厳密
- **レシピのエクスポート**: 必ず`recipes/index.ts`でエクスポート
- **ライト/ダーク**: セマンティックトークンは両方を定義

## ベストプラクティス

1. **セマンティックトークン優先**: 直接色を指定しない
2. **レシピを活用**: 再利用可能なパターンはレシピ化
3. **OKLCH使用**: 新しい色はOKLCHで定義
4. **型安全性**: MCPツールを活用して型エラーを防ぐ
5. **命名規則**: トークンはケバブケース、レシピはキャメルケース

## 詳細ドキュメント

- **SKILL.md**: 実装ガイド、デザインシステムの構造、実行時の注意点
- **FORMS.md**: APIリファレンス、具体的な使用例、トラブルシューティング

## サポート

質問や問題がある場合は、以下を確認するのだ：

1. `SKILL.md`で概要を確認
2. `FORMS.md`で具体例を確認
3. `/design-system check all`で問題をチェック
4. 型エラーはMCPツールで診断

## バージョン

- **v1.0.0**: 初版リリース
  - カラー管理
  - レシピ管理
  - トークン管理
  - コンポーネント生成
  - 検証・チェック機能

## ライセンス

このskillはTakakiAraki09プロジェクトの一部なのだ。

---

作成者: Claude Code
最終更新: 2026-01-18
