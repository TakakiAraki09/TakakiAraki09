import { styled } from "~/styled-system/jsx";
import {
  comboboxStyle,
  comboboxInputStyle,
  comboboxListStyle,
  comboboxOptionStyle,
} from "~/styles/combobox";

export const Combobox = styled("div", comboboxStyle);
export const ComboboxInput = styled("input", comboboxInputStyle);
export const ComboboxList = styled("ul", comboboxListStyle);
export const ComboboxOption = styled("li", comboboxOptionStyle);
