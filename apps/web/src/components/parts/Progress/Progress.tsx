import { styled } from "~/styled-system/jsx";
import { progressStyle, progressBarStyle } from "~/styles/progress";

export const Progress = styled("div", progressStyle);
export const ProgressBar = styled("div", progressBarStyle);
